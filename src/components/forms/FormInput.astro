---
interface Props {
  label: string;
  name: string;
  type?: "text" | "email" | "number" | "date" | "datetime-local" | "url";
  value?: string | number;
  required?: boolean;
  error?: string;
  placeholder?: string;
  min?: string | number;
  max?: string | number;
  step?: string | number;
}

const {
  label,
  name,
  type = "text",
  value = "",
  required = false,
  error,
  placeholder,
  min,
  max,
  step,
} = Astro.props;
---

<div class="mb-4">
  <label for={name} class="mb-2 block text-sm font-medium text-gray-700">
    {label}
    {required && <span class="text-red-500">*</span>}
  </label>
  <div class="relative">
    {
      Astro.slots.has("start") && (
        <div class="pointer-events-none absolute inset-y-0 left-0 flex items-center pl-3 text-gray-400">
          <slot name="start" />
        </div>
      )
    }
    <input
      type={type}
      id={name}
      name={name}
      value={value}
      required={required}
      placeholder={placeholder}
      min={min}
      max={max}
      step={step}
      class:list={[
        "focus:ring-secondary w-full rounded-lg border py-2 transition-colors focus:border-transparent focus:ring-2",
        Astro.slots.has("start") ? "pl-10" : "pl-4",
        Astro.slots.has("end") ? "pr-10" : "pr-4",
        error ? "border-red-500" : "border-gray-300",
      ]}
    />
    {
      Astro.slots.has("end") && (
        <div class="pointer-events-none absolute inset-y-0 right-0 flex items-center pr-3 text-gray-400">
          <slot name="end" />
        </div>
      )
    }
  </div>
  {error && <p class="mt-1 text-sm text-red-500">{error}</p>}
</div>
